# Install virtualenv and create a virtual environment
pip install virtualenv
virtualenv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate

# Install Django
pip install django

# Create a new Django project
django-admin startproject restaurant_booking

# Create a new Django app
python manage.py startapp restaurant

# Create database tables
python manage.py makemigrations
python manage.py migrate

# Create a superuser to access the admin panel
python manage.py createsuperuser

# Run the server
python manage.py runserver


# Create superuser
python manage.py createsuperuser

username = dominic
Email address: dominicmburu034@gmail.com
password = 1234


migrates
python manage.py makemigrations restaurant
# First apply the content types migrations (which the auth app depends on)
python manage.py migrate contenttypes

# Then apply auth migrations
python manage.py migrate auth

# Now apply your restaurant app migrations that contain the CustomUser model
python manage.py migrate restaurant

# Finally apply all remaining migrations
python manage.py migrate